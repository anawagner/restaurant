{{!-- menuItem.hbs --}}

{{#each menuItems}}
  <div class="menu-item">
    <img src={{image}} alt={{name}}>
    <div>
    <h3>{{name}}</h3>
    <span class="price">{{price}}</span>
    <p>{{description}}</p>
    </div>
  </div>
{{/each}}