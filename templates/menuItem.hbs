{{!-- menuItem.hbs --}}

{{#each menuItems}}
  <div class="menu-item">
    <img src={{image}} alt={{name}}>
    <h3>{{name}}</h3>
    <p>{{description}}</p>
    <span class="price">{{price}}</span>
  </div>
{{/each}}